rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow open access for development.
    // Consider tightening these rules for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
